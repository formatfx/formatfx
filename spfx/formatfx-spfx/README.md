# formatfx-spfx

The SharePoint Framework (SPFx) Command Set that puts the FormatFX Format
panel into a list's command bar. See `spfx/README.md` for the project's
setup, build, and deployment instructions.
